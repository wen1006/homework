F(N)=1+2+3+.....+N
=N+(N-1)+......+1
F(N)=N+F(N-1) 
F(1)=1
========================================================
[C]

#include <stdio.h>
#include<stdlib.h>
int fuc(int n){
	if (n==1) return 1;
	else
	return n+fuc(n-1);
}
int main (){
  printf("請輸入一個數：");
  int n ;
  scanf("%d",&n); 
  printf("該數N!加法為：%d\n",fuc(n));
  system("pause");
  return 0;
}
========================================================
